#----------------------------------------------
# TODO:
#   Fix "if" conditions in each job, feels redundant.
#   There is alot of replication of steps here since Github actions seems limited.
#   Consider CircleCI.
#   Cache Poetry bin so it can be reused between jobs instead of installing.
#----------------------------------------------
name: edbook
on:
  push:
    paths-ignore:
      - "**/README.md"
    branches-ignore:
      - master
  pull_request:
    branches:
      - master
    types: [closed]
env:
  DEFAULT_BRANCH: master
jobs:
  setup:
    name: Setup and build
    runs-on: ubuntu-20.04
    steps:
      #----------------------------------------------
      # check-out repo and set-up python
      #----------------------------------------------
      - name: Check out repository
        uses: actions/checkout@v2
      - name: Set up python
        uses: actions/setup-python@v2
        with:
          python-version: 3.9
      #----------------------------------------------
      # install & configure poetry
      #----------------------------------------------
      - name: Install Poetry
        uses: snok/install-poetry@v1.1.1
        with:
          virtualenvs-create: true
          virtualenvs-in-project: true
      - name: debug-cache
        run: |
          ls -alR ./${{env.GITHUB_WORKSPACE}}
      #----------------------------------------------
      # load cached venv if cache exists
      #----------------------------------------------
      - name: Load cached venv
        id: cached_poetry_dependencies
        uses: actions/cache@v2
        with:
          path: .venv
          key: venv-${{ runner.os }}-${{ hashFiles('**/poetry.lock') }}
      #----------------------------------------------
      # Get cached build
      #----------------------------------------------
      - name: Load cached build
        id: cached_build
        uses: actions/cache@v2
        with:
          path: artifacts
          key: build-${{ runner.os }}-${{ hashFiles('docs') }}
      #----------------------------------------------
      # install dependencies if cache does not exist
      #----------------------------------------------
      - name: Install dependencies
        run: poetry install
        if: steps.cached_poetry_dependencies.outputs.cache-hit != 'true'
      #----------------------------------------------
      # Build
      #----------------------------------------------
      - name: build
        if: steps.cached_build.outputs.cache-hit != 'true'
        run: |
          ls -al .
          source .venv/bin/activate
          poetry run hicli build -b dirhtml
          ls .
      - name: zip-build
        if: steps.cached_build.outputs.cache-hit != 'true'
        run: |
          ls -al .
          zip -r build-${{ runner.os }}-${{ hashFiles('docs') }} _build
      #----------------------------------------------
      # Store current branch name as output for reuse as dir name on remote host
      #----------------------------------------------
      - name: Extract branch name
        id: extract_branch
        shell: bash
        run: echo "::set-output name=branch_name::$(echo ${GITHUB_REF#refs/heads/})"
      #----------------------------------------------
      # Upload build artifact
      #----------------------------------------------
      - name: Upload build artifact
        uses: actions/upload-artifact@v2
        with:
          name: build-${{steps.extract_branch.outputs.branch_name}}
          path: artifact/build-${{steps.extract_branch.outputs.branch_name}}.zip
  tag-and-release:
    needs: setup
    name: Tag and release
    runs-on: ubuntu-20.04
    steps:
      - name: Check out repository
        uses: actions/checkout@v2
      - name: Bump version and push tag
        id: bump_version
        uses: anothrNick/github-tag-action@1.26.0
        env:
          GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}
          WITH_V: true
          DEFAULT_BUMP: patch
      #----------------------------------------------
      # Get build artifact from build job
      #----------------------------------------------
      - name: Get build artifact
        uses: actions/download-artifact@v2
        with:
          name: build-${{needs.build.outputs.branch_name}}
      - run: ls ./${{env.GITHUB_WORKSPACE}}
      #----------------------------------------------
      # zip build artifact
      #----------------------------------------------
      - run: zip -r build ./${{env.GITHUB_WORKSPACE}}
        name: Zip build output
      - name: Create pre-release
        if: needs.build.outputs.branch_name != 'master'
        id: create_prerelease
        uses: actions/create-release@v1
        env:
          GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}
        with:
          tag_name: ${{ steps.bump_version.outputs.new_tag }}
          release_name: ${{ steps.bump_version.outputs.new_tag }}
          draft: false
          prerelease: true
      - name: Create release
        if: needs.build.outputs.branch_name == 'master'
        id: create_release
        uses: actions/create-release@v1
        env:
          GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}
        with:
          tag_name: ${{ steps.bump_version.outputs.new_tag }}
          release_name: ${{ steps.bump_version.outputs.new_tag }}
          draft: false
          prerelease: false
      - name: Upload pre-release asset
        if: needs.build.outputs.branch_name != 'master'
        id: upload_prerelease_asset
        uses: actions/upload-release-asset@v1
        env:
          GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}
        with:
          upload_url: ${{ steps.create_prerelease.outputs.upload_url }}
          asset_path: ./build.zip
          asset_name: build-${{ steps.bump_version.outputs.tag }}.zip
          asset_content_type: application/zip
      - name: Upload release asset
        if: needs.build.outputs.branch_name == 'master'
        id: upload_release_asset
        uses: actions/upload-release-asset@v1
        env:
          GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}
        with:
          upload_url: ${{ steps.create_release.outputs.upload_url }}
          asset_path: ./build.zip
          asset_name: build-${{ steps.bump_version.outputs.tag }}.zip
          asset_content_type: application/zip
  deploy-feature-branch:
    if: ${{needs.build.outputs.branch_name != 'master'}}
    needs: [setup, tag-and-release]
    name: Deploy feature branch
    runs-on: ubuntu-20.04
    steps:
      #----------------------------------------------
      # Get build artifact from build job
      #----------------------------------------------
      - name: Get build artifact
        uses: actions/download-artifact@v2
        with:
          name: build-${{needs.build.outputs.branch_name}}
      #----------------------------------------------
      # Delete dir on remote server, upload build tar and extract on remote server.
      #----------------------------------------------
      - name: Cleanup and deploy feature
        id: cleanup_deploy_feature
        uses: appleboy/scp-action@master
        with:
          host: ${{ secrets.SSH_HOST }}
          username: ${{ secrets.HI_USERNAME }}
          password: ${{ secrets.HI_PASSWORD }}
          port: 22
          rm: true
          source: build-${{needs.build.outputs.branch_name}}
          target: ".public_html/staging/edbook/${{ needs.build.outputs.branch_name }}"
  deploy-production-branch:
    if: ${{needs.setup.outputs.branch_name == 'master'}}
    needs: [setup, tag-and-release]
    name: Deploy production branch
    runs-on: ubuntu-20.04
    steps:
      #----------------------------------------------
      # Get build artifact from build job
      #----------------------------------------------
      - name: Get build artifact
        uses: actions/download-artifact@v2
        with:
          name: build-${{needs.build.outputs.branch_name}}
      - name: Cleanup and deploy production
        id: cleanup_deploy_production
        uses: appleboy/scp-action@master
        with:
          host: ${{ secrets.SSH_HOST }}
          username: ${{ secrets.HI_USERNAME }}
          password: ${{ secrets.HI_PASSWORD }}
          port: 22
          rm: true
          source: html
          target: ${{ secrets.HI_PRODUCTION_PATH }}
